# Base URL of the FastAPI backend. Read on the server only; it is never sent to the browser.
# Local default: the backend started with `uvicorn app.main:app` (port 8000).
# Copy this file to .env.local to override it (do not commit .env.local).
API_BASE_URL=http://127.0.0.1:8000
